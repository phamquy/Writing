# semicolons

Không giống như rất nhiều ngôn ngư khác, Swift không bắt buộc phải có dấu chấm phẩy (;) ở cuối câu lệnh, mặc dù ta có thể thêm nếu muốn. Tuy nhiên, dấu (;) là bắt buộc nếu ta muốn viểt nhiều câu lệnh trên cùng một dòng.

```swift
let cat = "cat"; print(cat)
// Prints "cat"
```
